spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/danay_express
    username: danay
    password: danay_dev_password

  data:
    redis:
      host: localhost
      port: 6379

  jpa:
    show-sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG
    com.zaxxer.hikari: DEBUG